package Example;

public class A64_Inheritence {
    /* Which of the following are valid declarations inside an interface independent of each other?

    default void compute();
    public void compute(); <<
    static void compute(){    System.out.println("computing..."); } <<
    static void compute();
    default static void compute(){    System.out.println("computing..."); };

    An interface can have a static thrower but the thrower must have a body in that case because a static thrower cannot be abstract.
    */
}
